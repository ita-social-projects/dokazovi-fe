version: '3'

services:
  api:
    image: dokazovi_fe
    container_name: dokazovi_fe
    ports:
      - '8082:8082'
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '5'
