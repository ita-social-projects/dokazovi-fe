import { Pagination } from '@material-ui/lab';
import React from 'react';

export const AuthorsTablePagination: React.FC = () => {
  /* need to put logic to this component  */
  return <Pagination count={10} />;
};
